# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0

# Data & ML
numpy>=1.26.0
pydantic>=2.7.0

# LLM inference (ROCm-compatible)
vllm>=0.5.0
transformers>=4.41.0
tokenizers>=0.19.0

# Optional: accelerated inference
accelerate>=0.30.0

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23.0
